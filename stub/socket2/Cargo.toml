[package]
name = "socket2"
version = "0.5.6"
edition = "2021"
description = "WASM stub of socket2 for sui wasm builds"
license = "Apache-2.0"
publish = false

[lib]
path = "src/lib.rs"

[features]
default = []
all = []
